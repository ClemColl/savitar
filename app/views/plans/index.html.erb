<p id="notice"><%= notice %></p>

<h1>Plans</h1>

<%# <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Budget</th>
      <th>Duration</th>
      <th>Rh</th>
      <th>Answer</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody> %>
  <% @plans.order(:id).each do |plan| %>

    <% question = plan.answer.question%>
      <div><%= question.id%>. <%= question.text %></div>
      <div><span style="font-weight:bold;"><%= plan.answer.text %></span></div>
      <div><%= plan.name %></div>
      <div><%= link_to 'Show', plan %> | <%= link_to 'Edit', edit_plan_path(plan) %> | <%= link_to 'Destroy', plan, method: :delete, data: { confirm: 'Are you sure?' } %></div>
      <br>
  <% end %>
  <%# </tbody>
</table> %>

<%= link_to 'New Plan', new_plan_path %>
